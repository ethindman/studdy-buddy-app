section.row.light.no-margin
  .col.s12
    .title-wrapper.center-align
      h4.align-center StudyBuddy
      p Keep the phrases you learn <br> organized in one place
    .input-field
      input.validate(ng-model="user.email", placeholder="Email", id="email", type="text" required)
      input.validate(ng-model="user.password", placeholder="Password", id="password", type="password" required)
      input.btn.waves-effect.waves-light.full-width(ng-click="login(user)", type="submit", value="Sign in")
div.row.more-actions.center-align
  a(href="/register") Sign up?